dependencies {
    all_modules.split(',').each {
        if (!it.contains("experiments") && !it.contains("tests") && !it.contains("data2viz") && !it.contains("ex-")) {
            compile project(":d2v-$it-common")
        }
    }
}

