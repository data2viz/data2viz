def dependsOnProjects = [':axis', ':color', ':core-js', ':dsv', ':format', ':interpolate', ':random', ':request', ':scale', ':svg', ':tile', ':voronoi']

dependencies {

    dependsOnProjects.forEach {
        compile project(it)
    }

}

