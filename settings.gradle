include 'axis'
include 'color'
include 'dsv'
include 'experiments'
include 'interpolate'
include 'random'
include 'request'
include 'scale'
include 'shape'
include 'svg'
include 'tile'

includeMulti('core')
includeMulti('format')
includeMulti('path')
includeMulti('tests')
includeMulti('voronoi')


def includeMulti(String path){
    new File(path).eachDir{ includeAndNormalizeName(path + '/' + it.name)}
}

def includeAndNormalizeName(String path) {
    def name = path.substring(path.lastIndexOf('/')+1)
    println name
    include(name)
    project(":$name").projectDir = file(path)
}
