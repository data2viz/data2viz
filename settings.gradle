includeMulti 'axis'
includeMulti 'dsv'
includeMulti 'experiments'
includeMulti 'interpolate'
includeMulti 'request'
includeMulti 'scale'
includeMulti 'shape'
includeMulti 'svg'
includeMulti 'tile'

includeMulti('core')
includeMulti('color')
includeMulti('format')
includeMulti('path')
includeMulti('random')
includeMulti('tests')
includeMulti('voronoi')


def includeMulti(String path){
    new File(rootDir, path).eachDir{ includeAndNormalizeName(path + '/' + it.name)}
}

def includeAndNormalizeName(String path) {
    def name = path.substring(path.lastIndexOf('/')+1)
    println name
    include(name)
    project(":$name").projectDir = file(path)
}
