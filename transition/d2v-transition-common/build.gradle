dependencies {
    compile project(":ease:d2v-ease-common")
    compile project(":timer:d2v-timer-common")
    compile project(":viz:d2v-viz-common")
    testCompile project(":viz:d2v-viz-common")
}

kotlin {
    experimental {
        coroutines "enable"
    }
}