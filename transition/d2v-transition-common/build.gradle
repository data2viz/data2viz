dependencies {
    compile project(":ease:ease-common")
    compile project(":timer:timer-common")
    compile project(":viz:viz-common")
    testCompile project(":viz:viz-common")
}

kotlin {
    experimental {
        coroutines "enable"
    }
}